[project]
name = "agent-pipeline"
version = "0.1.0"
description = "Checkpointed research pipeline: document tree, ledger, reviews, hooks"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0"]

[tool.uv]
package = false
